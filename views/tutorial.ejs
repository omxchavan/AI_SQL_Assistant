<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="SQL Query Tester lets you write, run, and learn SQL queries online with AI-powered suggestions and real-time results."
    />
    <meta
      name="keywords"
      content="SQL, SQL tester, SQL online, run SQL queries, AI SQL, learn SQL"
    />
    <meta name="author" content="Om Chavan" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tutorial | SQL Query Tester</title>
    <link rel="icon" type="image/png" href="/favicon.png" />

    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />

    <!-- Icons -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />
    <link rel="stylesheet" href="/css/tutorial.css" />
  </head>
  <body>
    <nav class="navbar">
  <div class="nav-brand">SQL Query Tester</div>
  <ul class="nav-links">
    <li><a href="/">Home</a></li>
    <li><a href="/tutorial">Tutorial</a></li>
    <li><a href="/tester">Tester</a></li>
    <li><a href="/csv">CSV</a></li>
  </ul>
</nav>
    <header>
      <h1>
        <i class="fa-solid fa-database icon-blue"></i> SQL Query Tester –
        Tutorial
      </h1>
      <p>Master the app step by step and avoid common pitfalls 🚀</p>
    </header>

    <section>
      <h2>
        <i class="fa-solid fa-circle-info icon-blue"></i> 1. What This App Does
      </h2>
      <p>SQL Query Tester helps you:</p>
      <ul>
        <li>Write and run <strong>SQL queries directly</strong></li>
        <li>Use <strong>AI to generate SQL</strong> from natural language</li>
        <li>View query results in clean, formatted tables</li>
        <li>Manage demo database (reset / clear tables)</li>
      </ul>
    </section>

    <section>
      <h2>
        <i class="fa-solid fa-terminal icon-green"></i> 2. Using Direct SQL
      </h2>
      <ol>
        <li>Open the <strong>Direct SQL</strong> tab.</li>
        <li>Type or paste an SQL query.</li>
        <li>Click <strong>Execute Query</strong> to run it.</li>
        <li>Results will appear below.</li>
      </ol>
      <div class="note">💡 Example: <code>SELECT * FROM Books;</code></div>
    </section>

    <section>
      <h2>
        <i class="fa-solid fa-robot icon-blue"></i> 3. Using AI (Natural
        Language)
      </h2>
      <ol>
        <li>Go to the <strong>Natural Language</strong> tab.</li>
        <li>Type your question in plain English.</li>
        <li>Click <strong>Generate SQL</strong>.</li>
        <li>Review and edit if needed.</li>
        <li>Click <strong>Execute Generated SQL</strong> to run.</li>
      </ol>
      <div class="note">
        ✨ Example: Type <em>"Show all books with their authors"</em><br />
        AI generates:<br />
        <code>
          SELECT b.Title, a.FirstName, a.LastName<br />
          FROM Books b JOIN Authors a ON b.AuthorID = a.AuthorID;
        </code>
      </div>
    </section>

    <section>
      <h2>
        <i class="fa-solid fa-triangle-exclamation icon-yellow"></i> 4. Common
        Mistakes to Avoid
      </h2>
      <ul>
        <li>
          <strong>Case Sensitivity:</strong>
          Keywords (<code>SELECT</code>, <code>FROM</code>) are safe, but
          table/column names might be case sensitive. Example:
          <code>Books</code> ≠ <code>books</code>
        </li>
        <li>
          <strong>Missing Semicolons:</strong> Always end queries with
          <code>;</code>
        </li>
        <li><strong>Invalid Joins:</strong> Ensure join columns exist.</li>
        <li><strong>Empty Queries:</strong> Executing nothing will fail.</li>
        <li>
          <strong>AI SQL Edits:</strong> Double-check generated SQL for
          accuracy.
        </li>
      </ul>
    </section>

    <section>
      <h2>
        <i class="fa-solid fa-database icon-red"></i> 5. Database Management
      </h2>
      <p>
        In the <strong>Database Management</strong> panel, you can delete all
        tables.
      </p>
      <div class="danger">
        ⚠️ <strong>Warning:</strong> Deleting tables removes all data
        permanently. This cannot be undone.
      </div>
    </section>

    <section>
      <h2>
        <i class="fa-solid fa-lightbulb icon-green"></i> 6. Example Queries
      </h2>
      <ul>
        <li><code>SELECT * FROM Books;</code> – Show all books</li>
        <li>
          <code>
            SELECT b.Title, a.FirstName, a.LastName FROM Books b JOIN Authors a
            ON b.AuthorID = a.AuthorID;
          </code>
          – Books with authors
        </li>
        <li>
          <code>
            SELECT p.FirstName, p.LastName, b.Title, l.LoanDate FROM Loans l
            JOIN Books b ON l.BookID = b.BookID JOIN Patrons p ON l.PatronID =
            p.PatronID;
          </code>
          – Loan details with patrons
        </li>
      </ul>
    </section>

    <a href="/tester" class="back-link"
      ><i class="fa-solid fa-arrow-right"></i> Try SQL Query Tester</a
    >
    <footer>&copy; 2025 SQL Query Tester. Built with ❤️ for developers.</footer>
  </body>
</html>
